const config = {
  pages: [],
  fileName: 'tested-file',
  resultsDir: 'axe-results',
  basicAuth: null,
  axeConfig: {
    reporter: 'v2'
  },
  viewport: {
    width: '1920',
    height: '1080'
  }
}

module.exports = config
