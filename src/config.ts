const config = {
  pages: [],
  title: 'test title',
  fileName: 'tested-file',
  resultsDir: 'axe-results',
  basicAuth: null,
  axeConfig: {
    reporter: 'v2',
  },
  viewport: {
    width: '1920',
    height: '1080',
  },
  selectior: null,
};

export default config;
